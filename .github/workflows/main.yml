name: Terraform_fmt_check
on:
  push:
    branches:
      - actions-test
      - testing
  pull_request:
    branches:
      - testing
      - actions-test
jobs:
  linux:
    name: Linux build on node ${{ matrix.node_version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node_version: [8.x, 10.x]

    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Set Node.js version ${{ matrix.node_version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node_version }}

    - name: Install packages
      run: npm install terraform

    - name: Run Terraform fmt on push or PR
      run: sudo ./.github/workflows/scripts/terraform_fmt.sh